export enum CARD_ACTION {
    WRONG = 'WRONG',
    RIGHT = 'RIGHT',
    PAUSE = 'PAUSE',
}

export enum WRONG_VISIBILITY {
    SHOW = 'SHOW_WRONG',
    HIDE = 'HIDE_WRONG',
}

export enum DEFAULT_ACTION {
    RESET = 'reset',
}
